-# form to create/edit a slipbox
-# frozen_string_literal: true
= simple_form_for(@slipbox) do |f|
  .form-inputs
    = f.input :title
    = f.input :description
    -# = f.association :users, input_html: { class: 'select2' }, label_method: :username
    
  .form-actions
    = f.button :submit, class: 'btn btn-outline-primary'
