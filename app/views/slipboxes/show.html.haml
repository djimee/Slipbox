!!!
%html{:lang => "en"}
  %head
    %meta{:description => "Tree page for Slipbox"}/
    %title Threads
    = stylesheet_link_tag "trees"
    = javascript_include_tag "https://d3js.org/d3.v6.min.js"
  %body
    #wrapper
      = render "partials/navbar"

      %section#thread-tree

        %p#goback
          = link_to '< Back to slipboxes page', slipboxes_path

        %section#createTree
          %h2
            = link_to 'Create a new Tree', new_tree_path

        -# check if the slipbox has any trees, if it does, display them
        - if !@slipbox.trees.empty?
          %section#trees
            %h2 Trees
            %hr.solid/
            .slipFlexContainer
              - @slipbox.trees.each do |tree|
                %p Title:
                = link_to tree.title, tree_path(tree)
        - else
          %p There are currently no trees that belong to this slipbox. Create a tree 
        %svg
        %section
  / <haml_loud> javascript_include_tag "threads" </haml_loud>

