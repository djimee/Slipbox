!!!
%html{:lang => "en"}
  %head
    %meta{:description => "Tree page for Slipbox"}/
    %title Notes
    = stylesheet_link_tag "trees"
    = javascript_include_tag "https://d3js.org/d3.v6.min.js"

    #wrapper
      = render "partials/navbar"
      #content-wrapper

        %section#titleSection
          %p Currently viewing your Tree - "#{@tree.title}"
          -# NOTE NOTE NOTE NOTE can we make the tree title bold by any chance?
          %section#createNote
          %h2
            = link_to 'Create a new Note', new_note_path(tree_title: @tree.title)

          %p#goback
            = link_to '< Back to tree page', :back
      
        %section#notes

          #notesWrapper
            .noteWrapper
              - if !@tree.notes.empty?
                %section#notes
                  %h2 Notes
                  %hr.solid/
                  .slipFlexContainer
                    = render @tree.notes.reverse
          %svg
          %section
  / <haml_loud> javascript_include_tag "threads" </haml_loud>
