= simple_form_for(@tree) do |f|
  .form-inputs
    = f.input :title
    = f.association :slipbox, selected: :slipbox_title
    
  .form-actions
    = f.button :submit, class: 'btn btn-outline-primary'